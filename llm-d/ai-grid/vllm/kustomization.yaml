apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Shared kserve serving template (site-a/site-b). Render per site:
#   helpers/render.sh values.site-<s>.env vllm | oc apply -f -
# site-d serving is distro-specific -> vllm/site-d/.
resources:
  - serving.yaml
