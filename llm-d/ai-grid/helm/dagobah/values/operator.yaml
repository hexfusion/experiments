image: { repository: ghcr.io/praxis-proxy/grid-operator, tag: v0.1.4, pullPolicy: IfNotPresent }
swim:
  siteName: dagobah
  seeds: ""
  service: { enabled: true, type: ClusterIP, port: 7946 }
gateway:
  serviceName: consumer-gateway
  port: "8080"
