apiVersion: v1
kind: Service
metadata:
  name: grid-gateway
  namespace: grid-system
spec:
  selector:
    app: grid-gateway
  ports:
    - { name: https, port: 443, targetPort: 8443, protocol: TCP }
  type: ClusterIP
