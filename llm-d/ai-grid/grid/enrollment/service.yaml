apiVersion: v1
kind: Service
metadata:
  name: grid-enrollment
  namespace: grid-system
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: grid-enrollment
  type: ClusterIP
