apiVersion: v1
kind: Service
metadata:
  name: grid-gateway-extproc
  namespace: grid-system
spec:
  selector:
    app: grid-gateway-extproc
  ports:
    - { name: grpc, port: 9004, targetPort: 9004, protocol: TCP }
  type: ClusterIP
