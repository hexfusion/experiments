apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The grid's own CRDs (hand-rolled, not operator-installed). The operator watches
# these; apply them before it reconciles.
resources:
  - gridnetwork.yaml
  - gridsite.yaml
  - inferenceprovider.yaml
